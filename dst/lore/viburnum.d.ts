import { ICanon } from 'magikcraft.io';
export declare const name = "viburnum";
export declare const cost = 0;
export declare const code: (canon: ICanon) => (amount?: number, delay?: number) => void;
