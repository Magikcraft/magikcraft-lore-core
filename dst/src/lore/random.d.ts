import { ICanon } from '../magikcraft-types/canon';
export declare const cost = 0;
export declare const name = "random";
export declare const code: (canon: ICanon) => (min?: number, max?: number) => number;
