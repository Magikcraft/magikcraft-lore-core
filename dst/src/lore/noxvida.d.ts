import { ICanon } from '../magikcraft-types/canon';
/**
 * Demonstrates how to apply a potion effect to the player.
 */
export declare const name = "noxvida";
export declare const cost = 0;
export declare const code: (canon: ICanon) => (duration?: number) => void;
