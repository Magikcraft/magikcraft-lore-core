# Radiatum

`magikcraft.io.radiatum`

Make a player radioactive.

Arguments: playerName (`string`);
Returns: nothing

## Example

```javascript
const magik = magikcraft.io;

function glow(name) {
    magik.radiatum(name);
}
```